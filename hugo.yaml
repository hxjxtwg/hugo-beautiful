baseURL: "https://你的域名.com/"
title: "我的博客"
theme: "PaperMod"
languageCode: "zh-cn"
defaultContentLanguage: "zh-cn"

# 1. 强制生成搜索索引
outputs:
  home:
    - HTML
    - RSS
    - JSON

params:
  # 2. 强制整站使用亮色模式 (Typecho 风格)
  defaultTheme: light
  
  # 首页欢迎语
  homeInfoParams:
    Title: "你好 👋"
    Content: "这里是我的极简技术博客。"

  # 社交图标
  socialIcons:
    - name: github
      url: "https://github.com/你的用户名"

  # 侧边栏/顶部功能
  ShowShareButtons: true
  ShowReadingTime: true
  displayFullLangName: true
  ShowSearch: true

# 菜单
menu:
  main:
    - identifier: categories
      name: 分类
      url: /categories/
      weight: 10
    - identifier: tags
      name: 标签
      url: /tags/
      weight: 20
    - identifier: archives
      name: 归档
      url: /archives/
      weight: 30

# 👇👇👇 核心修改：让代码框变白！
markup:
  highlight:
    # 样式：friendly (灰白底色), tango (白底), trac (白底)
    # 推荐用 friendly，看着最舒服
    style: "friendly"
    
    # 必须为 false，否则会被主题的黑色背景覆盖！
    noClasses: false
    
    # 其他优化
    lineNos: true
    lineNumbersInTable: false
    guessSyntax: true
    tabWidth: 4